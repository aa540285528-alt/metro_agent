# Contributing

Do not commit credentials, production records, vector indexes, conversation data, or personal materials. Run `python -m ruff check src tests` and `python -m pytest -m "not live and not integration"` before opening a pull request.
