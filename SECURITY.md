# Security

Report suspected vulnerabilities privately to the repository maintainer. Never commit provider keys or database passwords. Production deployments must derive user identity from authenticated server-side credentials; client-supplied `user_id` is for local development only.
